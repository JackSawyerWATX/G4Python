# Python program to find the factorial of a 
# number using a built in function

import math

def factorial(n):
    return(math.factorial(n))

# Driver code
num = 16
print("Factorial of", num, "is", factorial(num))

# Time complexity: O(1)
# Auxiliary space: O(1)